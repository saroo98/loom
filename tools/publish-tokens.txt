# Forbidden tokens for loom_publish's firewall — one per line, case-insensitive
# substring unless prefixed with `re:` (then it's a Python regex, applied as-is).
#
# Add the things that must NEVER appear in a cut you publish: your name, your
# usernames, machine path fragments, private repo names, client/project names,
# domain words your owner-layer grew. Keep this file out of your own allowlist —
# it names the very things it keeps out.
#
# Examples (uncomment and replace):
# your-name
# your-github-username
# re:(?i)\bclient-codename\b
